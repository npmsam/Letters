name: Letters
version: "0.0.1"
main: com.sampreet.letters.Letters
api-version: "1.21"
prefix: Letters
authors: [ Sampreet Singh ]
description: Paper Minecraft plugin to customize system messages (join, quit, etc.) using & codes and MiniMessage, with PlaceholderAPI support and per-player, permission-group based, or global messages.
website: https://github.com/npmsam/Letters
softdepend:
  - PlaceholderAPI
  - LuckPerms
  - SuperVanish
  - PremiumVanish
commands:
  # Main command for controlling the plugin
  letters:
    description: The main command for managing the plugin.
    usage: /<command> <subcommand>
  # Main command for controlling the plugin
  tell:
    description: Command that overrides the vanilla /msg, /tell, and /w whisper commands.
    usage: /<command> <player> <message>
    aliases: [ "msg", "w" ]
permissions:
  # Permission to reload the plugin configuration
  letters.reload:
    description: Allows reloading of the plugin configuration.
    default: op